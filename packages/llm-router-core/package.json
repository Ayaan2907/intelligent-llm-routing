{
  "name": "llm-router-core",
  "version": "0.1.0",
  "private": true,
  "description": "Portable LLM preference profiles with a live, drift-checked model catalog and pluggable selector backends. Working name — final npm name pending availability check.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "typescript": "^5.9.2"
  }
}
