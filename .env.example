# Optional. Demo mode works with zero env vars set: the model catalog is a
# public OpenRouter endpoint and deterministic selection needs no key.
#
# Required for:
#   - /api/chat completions (routing a real prompt to a real model)
#   - the openrouter-auto selector backend
OPEN_ROUTER_API_KEY=
